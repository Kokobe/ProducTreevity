<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.productreevity">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProducTreevity">
        <activity android:name=".TeacherStats"></activity>
        <activity android:name=".StudentStats" />
        <activity android:name=".Timer" />
        <activity android:name=".Teacher" />
        <activity android:name=".Student" />
        <activity android:name=".Onboard1" />
        <activity
            android:name=".OnboardStart"
            android:label="@string/app_name"
            android:theme="@style/Theme.ProducTreevity.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SecondFragment" />
    </application>

</manifest>